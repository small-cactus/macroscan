// Simple test that doesn't require React Native
describe('Simple Test', () => {
  it('passes a dummy test', () => {
    // This is a dummy test that always passes
    expect(true).toBe(true);
  });
}); 